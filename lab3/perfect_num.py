for num in range(5, 10000):
    sumOfDivisors = 0
    currentDivisor = num // 2
    # loop until div >= 1
    while currentDivisor <= 1:
        
        # can num be divided evenly?
        if num % currentDivisor == 0:
            # update
            ...

        # update divisor

    # check if num == sum, print if yes
    if ... == ...:
        print(...)